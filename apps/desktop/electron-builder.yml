appId: com.lamonade.powerboard
productName: PowerBoard
copyright: © 2026 Kah Mun Lam
directories:
  buildResources: build
  output: release
files:
  - main.js
  - dist/**
  - package.json
asarUnpack:
  - "**/node_modules/sharp/**"
  - "**/node_modules/@img/**"
  # The rasterizer worker is spawned as a real process, so it has to be a real file on disk —
  # a plain Node child cannot read an entry inside app.asar. Unpacked here it also sits one level
  # above node_modules/sharp, which is exactly where its `import sharp` resolves from.
  - "dist/rasterizeWorker.mjs"
mac:
  category: public.app-category.graphics-design
  icon: build/icon.icns
  darkModeSupport: true
  extendInfo:
    ITSAppUsesNonExemptEncryption: false
mas:
  # Explicit, not inherited: electron-builder 26.15.x stops after signing the .app unless the pkg
  # target is named, and `fastlane mac beta` uploads a .pkg — without this the lane dies on a nil path.
  target: pkg
  type: distribution
  category: public.app-category.graphics-design
  entitlements: build/entitlements.mas.plist
  entitlementsInherit: build/entitlements.mas.inherit.plist
  provisioningProfile: build/PowerBoard_MAS.provisionprofile
  hardenedRuntime: false
