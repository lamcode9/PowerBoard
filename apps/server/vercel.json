{
  "installCommand": "npm install --workspace @board/schema --workspace @board/web",
  "buildCommand": "npm run build --workspace @board/schema && npm run build --workspace @board/web && rm -rf dist-web && cp -R ../web/dist dist-web",
  "outputDirectory": "dist-web"
}
