{
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace @powerboard/schema && npm run build --workspace @powerboard/renderers && npm run build --workspace @powerboard/server && npm run build --workspace @powerboard/web && rm -rf dist-web && cp -R ../web/dist dist-web",
  "outputDirectory": "dist-web",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api?path=:path*"
    },
    {
      "source": "/boards/:boardId/:folder/:path*",
      "destination": "/api/files?boardId=:boardId&folder=:folder&path=:path*"
    }
  ]
}
